// Prisma types are auto-generated, so we'll export the main types we need
export type { 
  Profile,
  Property,
  Order,
  Offer,
  VerificationToken,
  AuditLog,
  Valuation,
  Role,
  PropertyType,
  PropertyStatus,
  OrderStatus,
  OfferStatus,
  VerificationType,
} from '@prisma/client'